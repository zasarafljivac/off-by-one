{
    "tasks": [
        {
            "type": "vs64",
            "action": "build",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "label": "build project"
        },
        {
            "label": "build acme",
            "type": "shell",
            "command": "acme",
            "args": [
                "-f",
                "cbm",
                "-o",
                "${workspaceFolder}/build/off-by-one-acme.prg",
                "${workspaceFolder}/src/main-acme.asm"
            ],
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": []
        }
    ]
}
